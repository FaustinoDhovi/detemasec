'use client';

export default function StudentDashboard() {
  return (
    <div style={{ padding: '20px', textAlign: 'center' }}>
      <h1>Student Dashboard</h1>
      <p>This will show fees balance and transaction history</p>
      <p>First, complete the login system setup.</p>
    </div>
  );
}
